# Running the project on localhost

Since this project is multibrand react repo, run the project locally using following command:

$ npm run dev -- --scope temp-stores.basspro.com
or
$ npm run dev -- --scope temp-stores.cabelas.com

To generate test data use:

$ yext sites generate-test-data --scope temp-stores.basspro.com
